{
  "name": "cuchumbo-api",
  "version": "1.0.0",
  "scripts": {
    "deploy-dev": "serverless deploy --stage dev",
    "deploy-prod": "serverless deploy --stage prod",
    "test": "jest",
    "create-email-template": "cd email && aws ses create-template --cli-input-json file://cuchumboParticipant.json --profile cuchumbo-dev --region us-east-1",
    "update-email-template": "cd email && aws ses update-template --cli-input-json file://cuchumboParticipant.json --profile cuchumbo-dev --region us-east-1"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "serverless": "2.8.0",
    "serverless-domain-manager": "5.0.0"
  },
  "dependencies": {
    "seedrandom": "^3.0.5"
  }
}
